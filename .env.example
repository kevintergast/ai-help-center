# Nur Variablen-NAMEN. Echte Werte NICHT committen — lokal via `wrangler login`,
# in CI/Prod via GitLab-Protected-Variablen bzw. Cloudflare Secrets Store.

# Cloudflare
CLOUDFLARE_ACCOUNT_ID=

# Für CI-Deploy (nur in GitLab als protected+masked Variable, nicht lokal nötig):
# CLOUDFLARE_API_TOKEN=
